# نزلاوي

تطبيق مجتمع قرية النزل — ويب عربي RTL، يتثبّت على أندرويد وآيفون.

## التشغيل المحلي

```bash
npm install
npm run dev
```

## النشر على Vercel

المشروع جاهز لـ `npm run build` (Vite + Nitro). لا تستخدم أمر `vite build` مباشرة.

نسخة Flutter موجودة في مجلد `flutter/`.
